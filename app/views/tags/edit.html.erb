<h1>Edit Folder</h1>

<%= form_for @tag do |f| %>
  <div class="field">
    <%= f.label :name, class: 'label' %>
    <%= f.text_field :name, class: 'input' %>
  </div>

  <div class="field">
    <%= f.submit 'Save', class: 'button' %>
  </div>
<% end %>
