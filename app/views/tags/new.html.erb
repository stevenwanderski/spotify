<h1>Create Folder</h1>

<%= form_for @tag do |f| %>
  <div class="field">
    <%= f.label :name, class: 'label' %>
    <%= f.text_field :name, class: 'input', autofocus: true %>
  </div>

  <div class="field">
    <%= f.submit 'Save', class: 'button' %>
  </div>
<% end %>
